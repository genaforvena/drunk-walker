# Drunk Walker Server Environment Variables
# Copy this file to .env and update values as needed

# Server port (Railway/Render set this automatically)
PORT=3000

# Optional: Set a custom database path (default: server/walks.db)
# DATABASE_PATH=./walks.db
